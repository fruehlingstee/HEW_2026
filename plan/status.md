# CakeCanvas プロジェクトステータス

<<<<<<< HEAD
最終更新: 2026-09-10  
ステータス: 仕様書整合済み・開発土台の文書整備中(アプリ未構築)

## プロジェクト概要

CakeCanvas は、利用者が5号ケーキの色と飾りを指定し、3Dプレビュー上で自由に配置できるセミオーダーケーキサービスである。注文情報から店舗向けの俯瞰配置図と製造指示書を生成し、予約枠、テスト決済、店頭受取までを扱う。

Ver1.0では1店舗・1注文1ケーキ・店頭受取のみを実装する。登録済みパーツは常に注文可能とし、在庫数および提供可否は管理しない。
=======
最終更新: 2026-09-07  
ステータス: 仕様決定・基礎学習開始前

## 概要

CakeCanvasは、5号ケーキのスポンジ色・クリーム色を指定し、飾りを3Dプレビュー上で自由に配置できるセミオーダーケーキサービスである。注文情報から店舗向けの俯瞰配置図と製造指示書を生成し、予約枠、テスト決済、店頭受取までを扱う。

Ver1.0は1店舗・1注文1ケーキ・店頭受取のみ。登録済みパーツは常に注文可能とし、在庫数と提供可否は管理しない。
>>>>>>> 30a3f8c333cd9e5daad22e3a0d8bb53231c07596

## 担当

| 役割 | GitHubアカウント | 主な担当 |
|---|---|---|
| ① バックエンド・進行管理 | `fruehlingstee` | DB、注文、Stripe、環境構築、全体進行 |
| ② 店舗側フロント | `buna-bunaa` | 店舗画面、予約枠、製造指示書、①のレビュー補助 |
| ③ 3D・デザイン | `koyou` | Blender、R3F、3Dモデル、UIデザイン |
| ④ 買い手側フロント | `kobayashishuu0` | 買い手画面、Zustand、俯瞰SVG、3D連携 |

<<<<<<< HEAD
仕様と優先順位は4人全員の合意後に確定する。合意できない項目は実装せず、現在の仕様を維持する。

## 確定した開発方針

- 正式名称は **CakeCanvas**。リポジトリ名は `HEW_2026` のまま維持する。
- TypeScript、Next.js App Router、Tailwind CSS、shadcn/ui、Zod、react-hook-form、Vitestを採用する。
- パッケージ管理はnpm、Node.jsはLTSで固定する。
- サーバーデータはServer ComponentsとServer Actionsを基本とする。
- 日付処理はdate-fnsとdate-fns-tzに集約する。
- 日時はUTCで保存し、受取日はJSTの暦日、業務判定はAsia/Tokyoで行う。
- 内部IDはCUID、金額は円単位の整数で保存する。
- Prisma Migrateを使用し、マイグレーションファイルをGitで管理する。
- Local、Vercel Preview、Productionを分け、Preview DBとProduction DBを共有しない。
- 秘密情報をGitHub、Discord、ソースコードへ保存しない。`.env` 系ファイルはCodexの編集対象外とする。
- CIでは型チェック、ESLint、Vitest、Next.jsビルドを実行する。

## Git・Issue運用

- `main`を基準とし、ブランチ名は `feature/<Issue番号>-<説明>` とする。
- Conventional Commits、squash merge、PRレビュー1人以上を必須とする。
- GitHub Projectsは `Backlog → Ready → In Progress → Review → Done` で管理する。
- #24を親Issue、#4〜#23を直接の子Issueとして登録する。
- 親子関係は作業の包含、blocked-by関係は作業順序の表現に使い分ける。
- 完成時に各担当が報告する。時間の記録は行わないが、技術的に詰まった場合は完成前でも共有する。
- 週次進捗確認は毎週金曜日に30分行い、本ファイルへ進捗、次の作業、ブロッカーを記録する。

## 商品・画面仕様

- 1注文は1台の5号ケーキに固定する。
- 外周デコは提供しない。
- スポンジとクリームは利用者が自由に色を指定できる設計とする。
- Ver0.5ではプリセット3色で検証し、Ver1.0までにカラーピッカーへ移行する。
- 色は `#RRGGBB` で保存し、製造指示書に色見本と実物との差異に関する注記を載せる。
- 登録済みの飾りパーツは常に利用可能とし、提供可否や在庫数を持たない。
- 初期モデルは5号土台と苺だけでPoCを行い、成功後にラズベリー、ブルーベリー、チョコプレートを追加する。
- 初期実装上の飾り上限は12個とするが、種類と上限は後から変更できるデータ設計にする。
- メッセージプレートは300円、20文字、1行、書体1種類とする。改行、絵文字、機種依存文字は許可しない。

### 仮価格
=======
仕様と優先順位は4人全員の合意後に確定する。合意できない項目は実装せず現在の仕様を維持する。

## 技術・開発方針

- 正式名称はCakeCanvas。リポジトリ名は`HEW_2026`を維持する。
- TypeScript、Next.js App Router、Tailwind CSS、shadcn/ui、Zod、react-hook-form、Vitestを採用する。
- npmとNode.js LTSを使用する。
- Server ComponentsとServer Actionsを基本とし、サーバー状態管理ライブラリは初期導入しない。
- 日時はUTCで保存し、受取日と業務判定はAsia/Tokyoで扱う。date-fnsとdate-fns-tzを共通処理に使う。
- 内部IDはCUID、金額は円単位の整数とする。
- Prisma Migrateを使い、マイグレーションファイルをGitで管理する。
- Local、Vercel Preview、Productionを分け、Preview DBとProduction DBを共有しない。
- 秘密情報をGitHub、Discord、ソースコードへ保存しない。`.env`系ファイルはCodexの編集対象外とする。
- CIで型チェック、ESLint、Vitest、Next.jsビルドを実行する。
- APIエラー形式を統一し、決済、Webhook、返金結果はDBへ記録する。

## Git・Issue運用

- `main`を基準とし、ブランチは`feature/<Issue番号>-<説明>`とする。
- Conventional Commits、squash merge、1人以上のPRレビューを必須とする。
- GitHub Projectsは`Backlog → Ready → In Progress → Review → Done`で管理する。
- #24を親Issue、#4〜#23を直接の子Issueとして登録する。作業順序はblocked-by関係で表す。
- 毎週金曜日に30分の進捗確認を行い、本ファイルへ進捗、次の作業、ブロッカーを記録する。
- 作業時間は管理せず、各担当が完成時に報告する。ただし技術的に詰まった場合は完成前に共有する。

## 商品・画面仕様

- 1注文は5号ケーキ1台。外周デコは提供しない。
- スポンジ色とクリーム色は利用者が自由指定できる設計とする。Ver0.5はプリセット3色、Ver1.0はカラーピッカーを使う。
- 色は`#RRGGBB`で保存し、製造指示書に色見本と実物との差異に関する注記を載せる。
- 登録済みパーツは常に利用可能とし、在庫数・提供可否を持たない。
- 初期PoCは5号土台と苺。成功後にラズベリー、ブルーベリー、チョコプレートを追加する。
- 飾りの初期上限は12個だが、種類と上限は変更可能なデータ設計にする。
- メッセージプレートは300円、20文字、1行、書体1種類。改行、絵文字、機種依存文字は禁止する。

### 開発用の仮価格
>>>>>>> 30a3f8c333cd9e5daad22e3a0d8bb53231c07596

| 項目 | 税抜仮価格 |
|---|---:|
| 5号ケーキ | 3,500円 |
| 色指定 | 0円 |
| メッセージプレート | 300円 |
| 苺 | 200円 |
| ラズベリー | 120円 |
| ブルーベリー | 80円 |
| チョコプレート | 150円 |

<<<<<<< HEAD
価格は初期実装用の仮値であり、コア機能完成後に見直す。

## デザイン・3D

- `designs`を注文前に作成し、`placements.design_id`で配置を保持する。
- `orders`が`design_id`を参照し、注文確定時に`designs.locked_at`を設定する。
- 共有IDは推測困難なランダム値とする。
- 未注文デザインは7日、注文済みデザインは受取日の30日後まで保持する。
- 3D参考画像はサーバーへ保存せず、印刷時にブラウザで生成する。失敗時も俯瞰SVGだけで指示書を成立させる。
- 3Dモデルは通常のGLBから開始し、合計2MBを超えた場合のみDraco圧縮を検討する。
- 性能目標は初期表示3秒以内、操作中30fps以上、土台500KB以下、飾り1種類150KB以下とする。
- 3D縮退時は、SVG上の真上視点ドラッグ配置と簡易側面図による2.5D画面へ移行する。

## 注文・予約・決済

- 5号ケーキ1台を1製造ポイントとし、初期の日次上限は5ポイントとする。
- 受取枠は日単位で管理し、時間帯別にはしない。
- 飾り数は製造ポイントへ加算しない。
- 決済開始時に枠を15分間仮確保し、Stripeの`payment_intent.succeeded` Webhookで注文を確定する。
- 期限切れ仮確保は空き枠確認時にも解放し、Cronだけに依存しない。
- Stripeへの更新は注文IDを冪等性キーとして二重処理を防止する。
=======
種類・価格・上限はコア機能完成後に見直す。

## デザイン・3D

- `designs`を注文前に作り、`placements.design_id`で配置を保持する。
- `orders.design_id`で注文と結び、注文確定時に`designs.locked_at`を設定する。
- 共有IDは推測困難なランダム値。未注文デザインは7日、注文済みデザインは受取日の30日後まで保持する。
- 3D参考画像はサーバーへ保存せず印刷時に生成する。失敗時も俯瞰SVGだけで指示書を成立させる。
- 通常のGLBから始め、合計2MBを超えた場合だけDraco圧縮を検討する。
- 性能目標は初期表示3秒以内、操作中30fps以上、土台500KB以下、飾り1種類150KB以下。
- 3D縮退時はSVG上の真上視点ドラッグ配置と簡易側面図の2.5D画面へ移行する。

## 注文・予約・決済

- 5号ケーキ1台を1製造ポイントとし、初期の日次上限は5ポイント。
- 受取枠は日単位。飾り数は製造ポイントへ加算しない。
- 決済開始時に15分間仮確保し、Stripeの`payment_intent.succeeded` Webhookで注文を確定する。
- 期限切れ仮確保は空き枠確認時にも解放し、Cronだけに依存しない。
- Stripeへの更新は注文IDを冪等性キーとして二重処理を防ぐ。
>>>>>>> 30a3f8c333cd9e5daad22e3a0d8bb53231c07596

### 注文ステータス

`pending → confirmed → in_production → ready → completed`

<<<<<<< HEAD
例外状態は `cancelled`、`rejected`、`expired` とする。全遷移を`order_status_logs`へ記録する。

### 受取番号

- 表示形式は `MMDD-日別3桁連番`（例: `0907-023`）。
- 店舗、受取日、連番の組み合わせを一意にする。
- 店頭では下3桁と氏名または電話番号下4桁で照合する。
- 受取番号を注文照会やキャンセル認証には使用しない。

### 注文照会

- 32バイトの秘密トークンを発行し、DBにはSHA-256ハッシュを保存する。
- 注文照会はトークン単位で15分30回、キャンセル操作はトークンとIP単位で15分5回までとする。
- IPはハッシュ化して保存し、超過時はHTTP 429を返す。
- キャンセルは確認画面を経由し、注文と返金に一意制約を設けて二重実行を防ぐ。

## 個人情報・アレルゲン

- ゲスト注文では氏名、電話番号、メールアドレスを必須とし、住所とフリガナは取得しない。
- 受取日の30日後に個人情報を匿名化し、金額と決済・返金履歴は個人情報から分離して保持する。
- アレルゲン除外フィルタ、追加前警告、28品目の複雑なUIは実装しない。
- 注文確認画面と製造指示書に、実際に関係するアレルゲンと店舗注記を表示する。
- 注文確認時刻を`orders.allergen_confirmed_at`へ保存し、独立した同意履歴テーブルは作らない。
- 注意書きは「表示内容は店舗が登録した原材料情報に基づく参考情報です。同一設備で卵・乳・小麦を含む製品を製造しています。重篤なアレルギーをお持ちの方は注文前に店舗へご相談ください。」とする。

## 返金・通知

- 50%返金は税込総額の50%を端数切り捨てで算出する。
- 返金不可の場合も申請記録を残す。
- Stripe返金失敗は`failed`として記録し、店舗画面から再実行可能にする。
- プラットフォーム手数料は返金後に店舗へ残る金額の3%とする。
- メールは注文受付、受取可、購入者キャンセル、店舗却下・返金の4種類とする。
- 送信結果をDBへ記録し、同じメールの二重送信を防止する。

## デモ・展示

- デモモードはサーバー側設定でのみ有効化し、URLパラメータでは切り替えない。
- デモ注文はStripe、予約枠消費、個人情報入力をスキップし、`is_demo`を付与する。
- 展示終了後にデモデータを削除する。
- 展示構成はPC1台とモニター2台を基本とする。
- モニター1に買い手画面、モニター2に店舗画面と製造指示書を表示する。
- デモ店舗は「CakeCanvas Demo Store」、住所は「HEW展示会場」、電話番号は`000-0000-0000`とする。
=======
例外状態は`cancelled`、`rejected`、`expired`。全遷移を`order_status_logs`へ記録する。

### 受取番号・照会

- 受取番号は`MMDD-日別3桁連番`（例: `0907-023`）。店舗・受取日・連番を一意にする。
- 店頭では下3桁と氏名または電話番号下4桁で照合する。受取番号はWeb認証に使わない。
- 32バイトの秘密トークンを発行し、DBにはSHA-256ハッシュを保存する。
- 注文照会はトークン単位で15分30回、キャンセルはトークンとIP単位で15分5回まで。
- IPはハッシュ化し、超過時はHTTP 429を返す。キャンセルと返金は一意制約とStripe冪等性キーで二重実行を防ぐ。

## 個人情報・アレルゲン

- 氏名、電話番号、メールアドレスを必須とし、住所とフリガナは取得しない。
- 受取日の30日後に個人情報を匿名化し、金額と決済・返金履歴は分離して保持する。
- アレルゲン除外フィルタ、追加前警告、28品目の複雑なUIは実装しない。
- 注文確認画面と製造指示書に、実際に関係するアレルゲンと店舗注記を表示する。
- `orders.allergen_confirmed_at`を保存し、独立した同意履歴テーブルは作らない。
- 注意書き: 「表示内容は店舗が登録した原材料情報に基づく参考情報です。同一設備で卵・乳・小麦を含む製品を製造しています。重篤なアレルギーをお持ちの方は注文前に店舗へご相談ください。」

## 返金・通知

- 50%返金は税込総額の50%を端数切り捨てで算出する。返金不可でも申請記録を残す。
- Stripe返金失敗は`failed`として記録し、店舗画面から再実行可能にする。
- プラットフォーム手数料は返金後に店舗へ残る金額の3%。
- メールは注文受付、受取可、購入者キャンセル、店舗却下・返金の4種類。
- 送信結果をDBへ記録し、同じメールの二重送信を防ぐ。

## デモ・展示

- デモモードはサーバー側設定でのみ有効化する。
- デモ注文はStripe、予約枠消費、個人情報入力をスキップし、`is_demo`を付ける。
- 展示終了後にデモデータを削除する。
- PC1台とモニター2台を基本とし、買い手画面と店舗画面・指示書を分けて表示する。
- デモ店舗は「CakeCanvas Demo Store」、住所は「HEW展示会場」、電話番号は`000-0000-0000`。
>>>>>>> 30a3f8c333cd9e5daad22e3a0d8bb53231c07596

## 学習・マイルストーン

- 1〜2週目: Git、JavaScript、TypeScript、Reactの共通基礎。
<<<<<<< HEAD
- 3週目: Next.jsの共通基礎と担当別学習。
- 4週目以降: CakeCanvas上で担当別PoCを作る。
- Ver0.5ではプリセット色、苺の3D配置、座標保存・再表示、価格更新、Stripeテスト決済、注文一覧、俯瞰SVGを接続する。
- 2026-10-30にPCとiPhoneで3DのGo/No-Go判定を行う。
- 2027-01-15に画像印刷機能の採否を再判定する。
- 2027-01-31に機能を凍結し、以後はバグ、文言、価格、素材、性能の調整のみ行う。
- 2月に価格、返金、予約枠の自動テストと、注文から指示書までの結合テストを行う。

## 現在の保留事項

| 項目 | 判断期限 | 条件 |
|---|---|---|
| 画像印刷機能 | 2027-01-15 | コア機能完成、担当確保、著作権・保存・確認フローを決められる場合のみ採用 |
| HEW当日の役割分担 | 2027年2月 | 操作案内、店舗説明、障害対応を割り振る |
| モバイルルーターとローカル環境 | 2027年2月 | 回線断リハーサルまでに決定 |
| 3D検証用iPhone | 2026-10-30 | Go/No-Go判定までに借用を含めて確保 |
| 飾りの最終種類・価格・上限 | コア機能完成後 | 初期値は開発用の仮設定を使用 |
=======
- 3週目: Next.js共通基礎と担当別学習。
- 4週目以降: CakeCanvas上で担当別PoCを作る。
- 2026-10-30: PCとiPhoneで3DのGo/No-Go判定。
- 2027-01-15: 画像印刷機能の採否を再判定。
- 2027-01-31: 機能凍結。以後はバグ、文言、価格、素材、性能のみ調整。
- 2月: 価格、返金、予約枠の自動テストと、注文から指示書までの結合テスト。

## 保留事項

| 項目 | 判断期限 | 条件 |
|---|---|---|
| 画像印刷 | 2027-01-15 | コア完成、担当確保、著作権・保存・確認フローを決められる場合のみ |
| HEW当日の役割分担 | 2027年2月 | 操作案内、店舗説明、障害対応を割り振る |
| モバイルルーターとローカル環境 | 2027年2月 | 回線断リハーサルまでに決定 |
| 3D検証用iPhone | 2026-10-30 | Go/No-Go判定までに借用を含めて確保 |
| 飾りの最終種類・価格・上限 | コア機能完成後 | 開発中は仮マスタと上限12個を使用 |
>>>>>>> 30a3f8c333cd9e5daad22e3a0d8bb53231c07596

## 現在のブロッカー

- 開発環境とアプリケーションが未構築。
<<<<<<< HEAD
- 全員が採用技術を未学習または学習初期段階にある。
- 3D検証用iPhoneの確保方法が未定。

## 2026-09-10の作業記録

- 企画書と技術担当表の本文を、2026-09-07の決定に合わせて更新した。過去の決定履歴は履歴として保持する。
- README、開発手順、PRテンプレート、CIひな形を作成した。
- Node.jsの初期構成は24.21.0 LTSに固定した。
- アプリとpackage.json・lockfileは未作成。初期パッケージ追加の確認待ち。具体的な追加案は `docs/development.md` を参照する。
- CIは `ci.yml.example` のひな形であり未有効化。アプリの起動・型チェック・lint・テスト・ビルドは未検証。

## 次の作業

1. 初期パッケージ追加の確認後、開発構成案に沿ってアプリを初期化し、起動・型チェック・lint・Vitest・ビルドを検証してCIを有効化する。
=======
- 全員が採用技術を未学習または学習初期段階。
- 3D検証用iPhoneの確保方法が未定。

## 次の作業

1. README、ディレクトリ構成、PRテンプレート、CIの土台を整備する。
>>>>>>> 30a3f8c333cd9e5daad22e3a0d8bb53231c07596
2. 全員でGit、JavaScript、TypeScript、Reactの基礎学習を開始する。
3. `designs`、`placements`、`orders`、予約枠を中心にDBスキーマを設計する。
4. 5号土台と苺を表示する最小3D PoCを作る。
5. 5号ケーキの選択画面と仮価格計算を作る。
